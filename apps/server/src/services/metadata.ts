import type { AppDb } from '../db/index.js'

// ponytail: stub - Task 11 wires real metadata enrichment
export async function enrichMetadataAsync(_db: AppDb, _id: string, _url: string): Promise<void> {}
