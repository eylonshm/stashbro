### P2 Task 1 Report: XcodeGen Project Setup

**Status:** COMPLETE - BUILD SUCCEEDED

**Commit:** b425da9 - "feat(mac): XcodeGen project scaffold, openapi.yaml for swift-openapi-generator"

---

**Files created (15 total):**
- `apps/mac/project.yml` - XcodeGen config (exact brief content + product name fix)
- `apps/mac/.gitignore`
- `apps/mac/StashBro/StashBroApp.swift` - exact brief content
- `apps/mac/StashBro/Info.plist` - empty (xcodegen populated from project.yml)
- `apps/mac/StashBroTests/StashBroTests.swift` - exact brief content
- `apps/mac/StashBro/openapi.yaml` - copied from apps/server/openapi.json
- `apps/mac/StashBro/openapi-generator-config.yaml`
- `apps/mac/StashBro/Assets.xcassets/Contents.json` - minimal asset catalog
- `apps/mac/StashBro/AppDelegate.swift` - stub (required by StashBroApp.swift)
- `apps/mac/StashBro/SettingsView.swift` - stub (required by StashBroApp.swift)
- `apps/mac/StashBroShareExtension/ShareViewController.swift` - stub (required by NSExtensionPrincipalClass)
- `apps/mac/StashBroShareExtension/Info.plist` - populated by xcodegen
- `apps/mac/StashBro.xcodeproj/` - generated by xcodegen

**Build command used:**
```
xcodebuild -scheme StashBro -configuration Debug -destination 'platform=macOS' CODE_SIGN_IDENTITY=- CODE_SIGNING_ALLOWED=NO -skipPackagePluginValidation build
```
Result: `** BUILD SUCCEEDED **`

**Issues encountered and fixed:**

1. **Package product name mismatch** - Brief's `project.yml` used `- package: swift-openapi-runtime` and `- package: swift-openapi-urlsession` as dependency keys, but those packages export `OpenAPIRuntime` and `OpenAPIURLSession` as product names. Fixed by adding explicit `product:` keys in the dependencies section.

2. **Plugin validation** - `xcodebuild` refused to run the `OpenAPIGenerator` build tool plugin without the `-skipPackagePluginValidation` flag (Xcode security feature for CI headless builds). Added this flag; note that interactive Xcode builds prompt the user once and remember the answer.

3. **Missing stubs** - `StashBroApp.swift` references `AppDelegate` and `SettingsView` which don't exist yet (they're Phase 2 Task 2+ work). Created minimal stubs to make the skeleton compile. The share extension's `NSExtensionPrincipalClass` = `ShareViewController` similarly required a stub file.

**Package resolution:** SPM resolved all 5 declared packages plus transitive deps at build time (including swift-http-types, swift-argument-parser, OpenAPIKit, Yams, etc.).

**Notes for next tasks:**
- `AppDelegate.swift` and `SettingsView.swift` are stubs - replace in Task 2 (menubar/notch UI)
- `ShareViewController.swift` is a stub - replace in the Share Extension task
- The `openapi.yaml` file is JSON format with `.yaml` extension - swift-openapi-generator accepts this fine
- Hardened runtime is disabled for ad-hoc signing (expected in dev; enable for distribution with real team ID)

---

### Review Fixes (commit f2108d4)

**Critical fixed:** `StashBro.xcodeproj/` was committed; now ignored and untracked.

**Changes:**
- `apps/mac/.gitignore` - replaced partial xcodeproj ignores with `*.xcodeproj/` (full directory)
- `apps/mac/project.yml` - all 5 direct SPM deps pinned to `exactVersion` (resolved versions from Package.resolved):
  - GRDB: 6.29.3
  - swift-openapi-generator: 1.13.0
  - swift-openapi-runtime: 1.12.0
  - swift-openapi-urlsession: 1.3.1
  - KeyboardShortcuts: 2.4.0
- `git rm -r --cached apps/mac/StashBro.xcodeproj` removed all 3 xcodeproj files from index

**Verification:** `xcodegen generate && xcodebuild ... → BUILD SUCCEEDED`. `git ls-files apps/mac/StashBro.xcodeproj` returns empty. `git status` clean.

**Deferred (noted for later tasks):** share extension embed phase, openapi.yaml sync mechanism, operationIds.
